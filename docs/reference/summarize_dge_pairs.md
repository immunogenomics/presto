# Summarize differential gene expression pairs

Summarize differential gene expression pairs

## Usage

``` r
summarize_dge_pairs(dge_res, mode = c("min", "max")[1])
```

## Arguments

- dge_res:

  table returned by pseudobulk_deseq2() function when \`mode\` is
  \`pairwise\`

- mode:

  \-

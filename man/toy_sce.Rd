% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toy_data.R
\name{toy_sce}
\alias{toy_sce}
\title{Toy SingleCellExperiment object for examples and tests}
\usage{
toy_sce()
}
\value{
A \code{SingleCellExperiment} with \code{counts} and \code{logcounts} assays
and a \code{cell_type} colData column with values \code{"jurkat"} and \code{"t293"}.
}
\description{
Builds a small deterministic \code{SingleCellExperiment} (20 genes x 300
cells, two cell types in the \code{cell_type} colData column) with the
installed SingleCellExperiment version, so no serialized object needs to
ship with the package. The same simulated counts as \code{\link[=toy_seurat]{toy_seurat()}} are
stored in the \code{counts} assay, with log-normalized values in \code{logcounts}.
The generator restores the caller's random-number state.
}
\details{
Requires the suggested package \code{SingleCellExperiment} (Bioconductor).
}
\examples{
if (requireNamespace("SingleCellExperiment", quietly = TRUE)) {
    object_sce <- toy_sce()
    head(wilcoxauc(object_sce, "cell_type"))
}

}
\seealso{
\code{\link[=toy_seurat]{toy_seurat()}}, \code{\link[=wilcoxauc]{wilcoxauc()}}
}

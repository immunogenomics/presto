% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toy_data.R
\name{toy_seurat}
\alias{toy_seurat}
\title{Toy Seurat object for examples and tests}
\usage{
toy_seurat()
}
\value{
A \code{Seurat} object with \code{counts} and \code{data} layers and a
\code{cell_type} metadata column with values \code{"jurkat"} and \code{"t293"}.
}
\description{
Builds a small deterministic \code{Seurat} object (20 genes x 300 cells, two
cell types in the \code{cell_type} metadata column) with the installed Seurat
version, so no serialized object needs to ship with the package or be
updated when Seurat changes its internal class structure. Counts are
Poisson-simulated with a few upregulated marker genes per cell type, and
log-normalized into the \code{data} layer. The generator restores the caller's
random-number state, so calling it does not perturb reproducibility.
}
\details{
Requires the suggested package \code{Seurat}.
}
\examples{
if (requireNamespace("Seurat", quietly = TRUE)) {
    object_seurat <- toy_seurat()
    head(wilcoxauc(object_seurat, "cell_type"))
}

}
\seealso{
\code{\link[=toy_sce]{toy_sce()}}, \code{\link[=wilcoxauc]{wilcoxauc()}}
}
